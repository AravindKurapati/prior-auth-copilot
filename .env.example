# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Google Gemini — the only approved model provider for this project.
# Get a key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Optional overrides (defaults live in config/models.yaml)
# PA_MODEL_AGENT=gemini-flash-latest
# PA_MODEL_SUMMARIZER=gemini-flash-lite-latest

# Local paths (defaults shown)
# PA_STATE_DB=./.pa_state.db
# PA_MEMORY_DB=./.pa_memory.db
# PA_CHROMA_DIR=./.pa_chroma
