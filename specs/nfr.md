# Non-Functional Requirements — NFR-01 .. NFR-08

| ID | Requirement | Evidence / mechanism | Test | PR | Status |
|---|---|---|---|---|---|
| **NFR-01** | No secrets or API keys committed; env-var config with a committed `.env.example` | `.env.example` committed; `.env` in `.gitignore`; all keys read via `os.environ` in `config.py` | `tests/test_nfr01_no_secrets.py` — scans committed text for key-shaped strings, asserts no committed `.env`, asserts `.gitignore` excludes `.env` | scaffold, PR1 | done (.env.example + env-var config loader + `test_nfr01_no_secrets.py`) |
| **NFR-02** | Runs end-to-end from a single documented command with committed sample inputs and a README quick-start | `pac all`; `data/samples/*.json`; README "Quick start" | `tests/test_nfr02_smoke.py` — invokes the pipeline on a sample with mocked LLM, asserts a schema-valid `PADecision` | PR7 | pending |
| **NFR-03** | Untrusted free-text provider content is isolated (quarantine) and never trusted as instructions | `context/quarantine.py` — `quarantine_ref` is a derived label (`f"quarantine:{case_id}"`) that nothing dereferences; the raw text itself stays directly in `state["raw_provider_text"]`, and isolation instead comes from role isolation (only ever placed in a user-role message, never a system message) plus `context/assembly.py::select_for` excluding `raw_provider_text` from every downstream node's view — not from an actual store-and-reference indirection | `tests/test_nfr03_quarantine.py` — includes a prompt-injection canary sample; asserts disposition not forced; `traces/quarantine_canary.json` | PR5 | done (PR5a: quarantine mechanism — role isolation, HTML-escaping against delimiter forgery, preamble-checked audit helper. PR5b: `tests/test_nfr03_quarantine.py` drives the canary through the real compiled graph — `pa_copilot.graph.make_graph` — to a genuine terminal `decision`, proving the disposition isn't coerced by the injected instruction. Note: this row's evidence is `select_for`'s field exclusion + a message-capture assertion + the disposition check, not a `quarantine_flag` field — no such field exists anywhere in `src/`; that was this row's own stale wording, now corrected) |
| **NFR-04** | Structured JSON logs / traces of agent runs are committed as evidence | `tracing.py` writes one JSON trace per run to `traces/` | `tests/test_nfr04_trace_schema.py` — validates every `traces/*.json` against the trace schema | PR1, PR7 | partial (RunTracer landed; traces/ now has PR5a's `context_before_after.md` plus PR5b's `graph_topology.txt`, `pause_resume_transcript.md`, `run_full_case.json`, `route_clearcut.json`, `route_ambiguous.json`, `quarantine_canary.json` — all byte-stable, no embedded timestamps; full `test_nfr04_trace_schema.py` schema validation across all of `traces/` is still PR7's job) |
| **NFR-05** | All data synthetic; any PII synthetic and never written to logs in plaintext | synthetic generators in `data/synthetic/`; `tracing.py` redacts `member_id`-shaped tokens always, and unions `default_redact_values()` (provider names + member-id keys read from `data/synthetic/`) into the caller list so names are covered even with `redact_values=[]` | `tests/test_nfr05_redaction.py` — asserts `member_id` and synthetic provider names absent from the written trace with `redact_values=[]` | PR1 | done (RunTracer redaction) |
| **NFR-06** | The single-vs-multi-agent decision and the framework choice are documented with rationale | `docs/single-vs-multi-agent.md` (decision + `pac compare` observations), `docs/integration-decision.md` (MCP vs API vs direct-DB vs A2A), `docs/agent-patterns.md` | `tests/test_nfr06_docs_present.py` — asserts the docs exist and contain the required sections | PR2, PR7 | partial (integration-decision.md done + test; single-vs-multi-agent.md PR7) |
| **NFR-07** | Graceful degradation on tool/model failure: timeouts, retries, explicit exit conditions | `reflection.py` — `asyncio.wait_for` timeouts, `tenacity` backoff (max 3), `MAX_REPLANS` / `MAX_HOPS` / `recursion_limit`, model fallback to lite | `tests/test_nfr07_degradation.py` — a persistently failing tool ends at `human_review`, never an unhandled exception | PR6 | pending |
| **NFR-08** | Context-window management: summarization / compression for long threads | `context/summarization.py` wraps `langmem.short_term.SummarizationNode`; runs on every supervisor turn | `tests/test_nfr08_summarization.py` — a long synthetic thread triggers summarization; `state["context"]` running summary populated; `traces/context_before_after.md` shows token reduction | PR5 | done (PR5a: `context/summarization.py` wraps the real `SummarizationNode`; `tests/test_nfr08_summarization.py`; `traces/context_before_after.md` — 48→9 messages, 1488→313 tokens, byte-stable) — NOTE: the committed trace is a synthetic thread; nothing in PR5a's worker code yet writes to state["messages"], so this mechanism is currently inert inside the real system until PR5b's workers (or graph.py) populate that field. Tracked for PR5b. |

## Additional rubric-driven requirements (not numbered NFRs, still mandatory)

| Requirement | Evidence | PR |
|---|---|---|
| PR-driven Git history: >= 3 `git merge --no-ff` merges; no direct pushes to `main` | git log / `--first-parent` history | all |
| `docs/business-case.md` covering problem, actors, success metrics | committed doc | PR1 |
| Documented agent pattern (ReAct / plan-execute / reflection) with rationale | `docs/agent-patterns.md` | PR7 |
| 22-parameter rubric self-audit | `docs/rubric-coverage.md` | PR7 |
