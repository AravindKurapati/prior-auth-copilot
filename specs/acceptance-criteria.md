# Acceptance Criteria — AC-01 .. AC-12

Per the AC-Traceability Rule, each AC is referenced by at least one test **and** one
committed evidence artifact, both carrying the `AC-NN` identifier. Status is updated as PRs
land (see `docs/design.md` Section 10).

| ID | Criterion | Test(s) | Committed artifact(s) | PR | Status |
|---|---|---|---|---|---|
| **AC-01** | LangGraph with an explicit typed state object (TypedDict/Pydantic) shared across nodes | `tests/test_ac01_typed_state.py` — asserts `PACaseState` is a `TypedDict`, every node reads and writes it, `graph.get_graph()` introspection | `src/pa_copilot/state.py`, `src/pa_copilot/graph.py`, `traces/graph_topology.txt` | PR1, PR5 | done (PR1: schemas + state; PR5a: `intake`/`benefit_check`/`supervisor` built; PR5b: `graph.py` assembles all 7 nodes into the real `StateGraph`, `traces/graph_topology.txt` committed via `draw_mermaid()` — `draw_ascii()` proved non-deterministic, see design.md §1 note) |
| **AC-02** | Supervisor routes a PA request to specialized workers (intake, benefit-check, medical-necessity, decision-draft) | `tests/test_ac02_supervisor_routing.py` — supervisor emits `RouterDecision`; all four workers reachable; a run visits >= 2 workers via the supervisor | `traces/run_full_case.json` (`route_history`), `docs/single-vs-multi-agent.md` | PR5 | done (PR5a: supervisor + 2 workers; PR5b: full compiled-graph run visits 4 distinct workers — `intake`, `benefit_check`, `medical_necessity`, `decision_draft` — to `FINISH`, `traces/run_full_case.json`) |
| **AC-03** | Conditional edges route on state (auto-draft clear-cut approvals; ambiguous -> clinical review) | `tests/test_ac03_conditional_routing.py` — clear-cut case takes the auto path to `decision_draft`; ambiguous case routes to `human_review`; same compiled graph, different state | `traces/route_clearcut.json`, `traces/route_ambiguous.json` | PR5 | done (PR5b: same compiled graph, two scripted states — clear-cut reaches `decision_draft`/`FINISH`, ambiguous diverges at `medical_necessity` to `human_review` via a genuine LLM `RouterDecision`, not a guardrail short-circuit) |
| **AC-04** | Node/agent outputs are validated structured objects (Pydantic) at handoff boundaries | `tests/test_ac04_structured_output.py` — each worker returns a validated model; malformed output raises `ValidationError` and is handled | `src/pa_copilot/schemas.py`, typed artifacts inside `traces/run_full_case.json` | PR1, PR5 | done (PR1: schemas; PR5a: `intake`/`benefit_check` structured output; PR5b: a malformed structured-output response surfaces as `agents._react.WorkerOutputError` — not silently passed through, not mislabeled as a tool failure — verified through both `intake` (only catches `WorkerToolError`, so a validation failure propagates) and `decision_draft` (catches it, returns `needs_replan`)) |
| **AC-05** | A checkpointer persists graph state so a case can be paused and resumed | `tests/test_ac05_checkpointer.py` — run interrupts at `human_review`; a fresh graph object built from the same `AsyncSqliteSaver` resumes and completes | `traces/pause_resume_transcript.md` (two separate process invocations) | PR5 | done (PR5b: `AsyncSqliteSaver`, not the sync `SqliteSaver` — see design.md §1 correction — verified to pause at `human_review` and resume/complete from a fresh connection/graph object in a genuinely separate process; `scripts/run_pause_resume_test.py` → `traces/pause_resume_transcript.md`) |
| **AC-06** | Tiered memory (short-term working + long-term/semantic); recalls a fact from an earlier turn | `tests/test_ac06_tiered_memory.py` — fact stated on turn 1 is recalled on turn 3 from working memory | `traces/tiered_memory_recall.json` | PR4 | done (PR4: `memory/working.py` Tier-1 + `PolicyStore` Tier-2 semantic recall; `tests/test_ac06_tiered_memory.py`; `traces/tiered_memory_recall.json`) |
| **AC-07** | Memory persists across sessions: a committed test starts a new session and recalls prior-session facts; output log committed | `tests/test_memory_persistence.py` + `scripts/run_persistence_test.py` (two `python` processes, same on-disk store) | `traces/memory_persistence.log` | PR4 | done (PR4: in-process teardown+rebuild + 2-process `scripts/run_persistence_test.py`; `tests/test_memory_persistence.py`; `traces/memory_persistence.log`) |
| **AC-08** | A memory eviction/importance policy (TTL, LRU, or importance-weighted) is implemented and documented | `tests/test_ac08_eviction.py` — overflow a namespace: stale low-importance item evicted, `critical` retained, expired item gone after `sweep_ttl()` | `docs/memory-policy.md`, `src/pa_copilot/memory/policy.py` | PR4 | done (PR4: `memory/policy.py` TTL + importance + LRU cap; `docs/memory-policy.md`; `tests/test_ac08_eviction.py`) |
| **AC-09** | Custom MCP server exposes >= 2 tools and 1 resource relevant to the domain | `tests/test_ac09_mcp_server.py` — spin the server, list 3 tools (`benefit_lookup`, `criteria_check`, `provider_lookup`) + 1 resource (`pa://criteria/{policy_id}`), invoke each | `src/pa_copilot/mcp_server/server.py`, `traces/mcp_capabilities.json` | PR2 | done (PR2: FastMCP server 3 tools + pa://criteria resource; traces/mcp_capabilities.json) |
| **AC-10** | Agent consumes the MCP server via `langchain-mcp-adapters`; committed transcript shows the agent invoking an MCP tool | `tests/test_ac10_mcp_integration.py` — a real MCP-backed run of the `medical_necessity` worker factory (`build_medical_necessity_node`, called directly, not routed through the compiled `make_graph` StateGraph) invokes `criteria_check` through the adapter | `traces/mcp_toolcall_transcript.md`, `traces/mcp_tool_calls.jsonl` | PR2, PR7 | done (PR2: adapter integration + representative transcript; PR5b: `tests/test_ac10_mcp_integration.py::test_ac10_medical_necessity_worker_invokes_real_criteria_check_in_graph` calls `build_medical_necessity_node` — the exact factory `graph.py` wires into the compiled StateGraph, but exercised directly here, not via a `make_graph` run — with REAL MCP tools loaded through `pa_session`/`load_pa_tools`: a genuine stdio subprocess dispatches `criteria_check`, not a fake tool standing in. A full compiled-graph run of this path is not yet covered; that remains deferred, not attempted in this fix wave) |
| **AC-11** | Agentic-RAG tool the agent decides when to call for coverage-criteria and medical-necessity lookups (retrieval inside the loop) | `tests/test_ac11_agentic_rag.py` — indeterminate case calls `search_clinical_guidance`; clear-cut case does not | `traces/agentic_rag_decision.md` | PR3, PR5 | done (PR3: tool + predicate + Chroma index + decision trace; PR5b: `tests/test_ac11_agentic_rag.py` extended with a full in-graph proof — one compiled-graph run where the agent calls `search_clinical_guidance`, one where it doesn't, both through the real tool via a spy on `.invoke()`, not a re-test of the tool-level predicate alone) |
| **AC-12** | Reflection or self-healing/fallback loop (re-plan on tool failure or low-confidence output) with an evidenced trace | `tests/test_ac12_reflection.py` — 4 scenarios through the real compiled graph: tool-failure recovers/exhausts, low-confidence recovers/exhausts | `traces/reflection_tool_failure.json`, `traces/reflection_low_confidence.json` | PR6 | done (PR6: `agents/_react.py`'s `AttributedToolError` + `reflection.py`'s `attribute_tool_errors`/`run_worker_react_resilient` give real per-tool attribution + tenacity/timeout resilience; `supervisor.py`'s broadened `hard_route` cap + `build_supervisor_node`'s same-turn `replan_count` bookkeeping bound both triggers; `medical_necessity.py` flags `needs_replan` on low confidence/indeterminate without withholding `necessity`, letting the LLM router's existing fall-through choose to loop back — proven reachable via a scripted `RouterDecision` in the recovers case, not a new hard_route rule. `tests/_reflection_case.py` holds the 4 scripted scenarios; `scripts/reflection_demo.py` produces the 2 committed recover-case traces) |

## Notes

- "Session" for AC-07 = a fresh set of in-memory objects (graph, store, checkpointer)
  pointing at the same on-disk SQLite files, exercised both in-process (teardown + rebuild)
  and cross-process (`scripts/run_persistence_test.py`).
- Live-Gemini tests are `@pytest.mark.slow`; their committed `traces/` outputs are the
  scored evidence. CI runs the deterministic + mocked subset.
